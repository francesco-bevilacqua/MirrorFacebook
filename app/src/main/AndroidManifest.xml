<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comview.example.francesco.mirrorfacebook">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".MarketActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".StuffActivity" />
        <activity android:name=".ItemShowActivity" />
        <activity android:name=".ListOptionActivity" />
        <activity android:name=".ItemMarketActivity" />
        <activity android:name=".ItemNotify" />
        <activity android:name=".ActionProfile"></activity>
    </application>

</manifest>